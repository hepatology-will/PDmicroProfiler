% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction_network.R
\name{plot_interaction_network_static}
\alias{plot_interaction_network_static}
\title{Visualize bacteria-phage interaction network (static)}
\usage{
plot_interaction_network_static(
  interaction_data,
  correlation_threshold = 0.3,
  top_n_phage = 30,
  title = NULL,
  bacteria_color = "#D88090",
  phage_color = "#87DBCB"
)
}
\arguments{
\item{interaction_data}{Output from \code{find_phage_host_interactions()}.}

\item{correlation_threshold}{Numeric. Only edges with absolute correlation
greater than or equal to this value are shown. Default: \code{0.3}.}

\item{top_n_phage}{Integer. Maximum number of phage nodes to display (ranked
by absolute correlation, descending). Default: \code{30}.}

\item{title}{Character. Custom plot title. If \code{NULL} (default), a title
is generated automatically from the data.}

\item{bacteria_color}{Character. Fill colour for bacteria nodes (hex or named
colour). Default: \code{"#D88090"}.}

\item{phage_color}{Character. Colour for phage nodes (hex or named colour).
Default: \code{"#87DBCB"}.}
}
\value{
A \code{ggplot2} object that can be further modified or saved with
\code{ggsave()}.
}
\description{
Creates a static network plot using ggplot2 and igraph, suitable for saving
as PNG, PDF, or other raster/vector formats via \code{ggsave()}. Bacteria
nodes are placed at the centre; phage nodes with positive correlations are
arranged on the right arc and those with negative correlations on the left
arc. Edge width is proportional to the absolute correlation value.
}
\examples{
\dontrun{
interactions <- find_phage_host_interactions(
  bacteria_name = "Lachnospiraceae",
  groups = "PD",
  tax_level = "family",
  correlation_threshold = 0.3
)
p <- plot_interaction_network_static(interactions, correlation_threshold = 0.3)
ggsave("network_PD.png", p, width = 10, height = 10, dpi = 300)
}
}
